{
  "name": "single-endpoint-client",
  "version": "1.0.0",
  "description": "Dummy client for testing the new endpoint",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "cchu",
  "license": "ISC",
  "devDependencies": {
    "express": "^4.13.4"
  }
}
